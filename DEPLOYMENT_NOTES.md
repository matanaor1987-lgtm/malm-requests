# ⚠️ הערה קריטית — יש שני ריפואים/פריסות שונות למערכת מלמ

**תאריך תיעוד:** 2026-07-21

## הבעיה שהתגלתה

יש **שני ריפואים נפרדים לגמרי** שמריצים את מערכת בקשות מלמ, ושניהם מחוברים לאותו Firebase DB
(`https://malm-focus-default-rtdb.firebaseio.com`):

1. **`matanaor1987-lgtm/malm-requests`** (הריפו הזה) — נפרס ל:
   `https://matanaor1987-lgtm.github.io/malm-requests/`
2. **ריפו אחר, ישן יותר** ("work-arrangement") — נפרס ל:
   `https://matanaor1987-lgtm.github.io/work-arrangement/malm/index.html`

**הריפו שבפועל בשימוש היומיומי של המשתמשים הוא #2 (`work-arrangement`), לא הריפו הזה.**

## למה זה גרם לבלבול

פותחנו וטסטנו שיפורי UI רבים על `malm-requests` (הריפו הזה), דחפנו בהצלחה ל-`main`, ה-deploy
הצליח (מאומת דרך GitHub Actions logs — "pages build and deployment" מ-`main`, סטטוס success) —
אבל המשתמשים המשיכו לראות גרסה ישנה, כי הם בכלל לא נכנסים לכתובת של הריפו הזה. בזבזנו זמן רב
בחקירת "בעיית cache" של GitHub Pages/CDN/פרוקסי ארגוני, שלא הייתה קיימת בכלל — הבעיה האמיתית
הייתה שמדובר בשתי פריסות שונות לגמרי.

**רמז שהיה קיים כל הזמן בקוד ולא זוהה בזמן:** ב-`index.html` יש שורה שרושמת Service Worker
לנתיב `/work-arrangement/malm/sw.js` — נתיב ששייך לריפו האחר, לא לריפו הזה. זה כנראה שריד מקוד
שהועתק במקור מהריפו הישן.

## מה לעשות מעכשיו

- **לפני שמתחילים לעבוד על שינוי UI/פיצ'ר, לוודא קודם על איזה ריפו/URL עובדים בפועל** ולא
  להניח שזה `malm-requests` רק כי זה הריפו שיש לנו גישה אליו.
- קיבלנו גישה ל-`work-arrangement` ב-2026-07-21. יש לוודא אחד מהשניים:
  1. **לסנכרן** את כל השיפורים מ-`malm-requests` לתוך `work-arrangement/malm/index.html`, או
  2. **לאחד לריפו אחד** ולהפנות/למחוק את השני, כדי שלא יהיה בלבול עתידי בין שתי גרסאות שמצביעות לאותו DB.
- עד שזה יקרה, **כל שינוי שנעשה בריפו הזה (`malm-requests`) לא ישפיע על מה שהמשתמשים בפועל רואים**.

## ⚠️ קריטי: work-arrangement הוא ריפו משותף עם כמה מערכות שונות!

`matanaor1987-lgtm/work-arrangement` **הוא לא ריפו ייעודי למלמ** — זה ריפו שמכיל כמה מערכות
שונות ונפרדות שמתן בנה לצוות שלו, כולן חיות תחת אותו ריפו ואותו GitHub Pages deploy:

```
work-arrangement/
├── index.html                 ← מערכת אחרת של מתן (לא מלמ!)
├── michal_countdown_dhl.html  ← מערכת אחרת של מתן (לא מלמ!)
├── batch/                     ← מערכת אחרת של מתן (לא מלמ!)
├── customs/                   ← מערכת אחרת של מתן (לא מלמ!)
├── export/                    ← מערכת אחרת של מתן (לא מלמ!)
├── quality/                   ← מערכת אחרת של מתן (לא מלמ!)
├── robot/                     ← מערכת אחרת של מתן (לא מלמ!)
├── דגכ/                       ← מערכת אחרת של מתן (לא מלמ!)
└── malm/                      ← **רק זה** שייך למערכת בקשות מלמ
    ├── index.html
    ├── config.js
    ├── sw.js, manifest.json, icons
    └── portal.html, request.html, track.html, migrate.html (עמודים נוספים של מלמ)
```

**כלל ברזל: כל שינוי בריפו `work-arrangement` חייב להיות מוגבל אך ורק לקבצים בתוך `malm/`.**
לעולם לא לגעת ב-root או בתיקיות האחרות — אלה לא שלנו ופגיעה בהן משפיעה על מערכת אחרת לגמרי
שצוות שלם אחר תלוי בה.

**נהלי בטיחות בעבודה על הריפו הזה:**
1. `git add` תמיד עם שם קובץ מפורש (`git add malm/xxx`) — לעולם לא `git add -A` / `git add .`
2. לבדוק `git status` / `git diff --stat` לפני כל commit ולוודא שרק קבצים תחת `malm/` השתנו
3. `git fetch` לפני כל `push` כדי לא לדרוס שינוי מקביל של מתן במקום אחר בריפו
4. בלי פעולות גורפות (מחיקות, rename, כלי automation שרצים על כל הריפו)

## עדכון אבטחה — 2026-07-21

התגלה טוקן GitHub (Personal Access Token) חשוף בטקסט גלוי בקובץ `config.js`, בשני הריפואים
(`malm-requests` וגם `work-arrangement/malm`). הטוקן שימש להעלאת קבצים מצורפים דרך קריאה
ישירה מהדפדפן ל-GitHub API. מכיוון שהאתר סטטי (GitHub Pages, בלי backend), כל secret בקוד
הלקוח חשוף לכל מי שנכנס לאתר — "פיצול" הטוקן לשני משתנים לא מהווה הגנה אמיתית.

**הטיפול:** הבעלים ביטל (Revoke) את הטוקנים, והם הוסרו מהקוד בשני הריפואים (רק `config.js`,
שינוי חד-קובצי בכל ריפו). **תוצאה: העלאת קבצים מצורפים מושבתת כרגע בשתי המערכות**, עד
שיוגדר מנגנון מאובטח חלופי (למשל Firebase Storage, שכבר בשימוש לשאר הנתונים — עדיף על
העלאה ישירה ל-GitHub עם secret בצד לקוח).
